---
import Layout from '../layouts/Layout.astro'
import PageBuilder from '../components/PageBuilder.astro'
import { getTeamPage, getSiteSettings } from '../utils/sanity'

const page = await getTeamPage(Astro.cookies)
const settings = await getSiteSettings(Astro.cookies)

const seo = page?.seo ?? {}
const blocks = page?.content ?? []
---

<Layout
  seo={seo}
  siteName={settings?.siteName}
  ogImage={seo?.ogImageUrl ?? settings?.defaultSeo?.ogImageUrl}
>
  <PageBuilder blocks={blocks} />
</Layout>
